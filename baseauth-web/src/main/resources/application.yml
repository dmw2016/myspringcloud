###服务启动端口号
server:
  port: 8400
###服务名称(服务注册到eureka名称)
spring:
  application:
    name: baseauth-web
###服务注册到eureka地址
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8100/eureka
swagger:
  base-package: com.baseauth.web.controller
  title: BaseAuth开发项目
  version: 1.1
  contact:
    name: 上海小组
  enabled: true
